public class collectionsBasics {
  public static void main(String[] args) {
    // collection API -> collection and collections
    // collection -> interface -> implemented by various classes such as List, Set,
    // Map, Queue
    // collections -> class -> provides various static methods to perform operations
    // on collections such as sorting, searching, etc.

    // collection API in java is a set of classes and interfaces that provide a way
    // to store and manipulate groups of objects. it is part of the java.util
    // package and provides a way to organize and manage data efficiently. the
    // collection API includes several classes and interfaces, such as List, Set,
    // Map, and Queue, which are used to store and manipulate different types of
    // collections. the collection API also provides various methods to perform
    // operations on collections, such as adding, removing, and searching for
    // elements.

    // collection is a interface in java that represents a group of objects. it is
    // the root interface of the collection hierarchy and is implemented by various
    // classes, such as List, Set, and Queue.

    // collections in java are used to store and manipulate groups of objects. they
    // provide a way to organize and manage data efficiently. there are several
    // types of collections in java, including lists, sets, maps, and queues.

    // list is an ordered collection that allows duplicate elements. it is
    // implemented
    // by the ArrayList and LinkedList classes.

    // set is an unordered collection that does not allow duplicate elements. it is
    // implemented by the HashSet and TreeSet classes.

    // map is a collection that maps keys to values. it is implemented by the
    // HashMap and TreeMap classes.

    // queue is a collection that follows the first-in-first-out (FIFO) principle.
    // it is implemented by the LinkedList class.

    // we can also use the Collections class to perform various operations on
    // collections, such as sorting, searching, and shuffling. for example,
    // Collections.sort(list) will sort the elements of the list in natural order.
  }
}